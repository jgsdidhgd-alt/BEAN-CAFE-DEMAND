 "use client"; import {useEffect,useState} from "react"; import {useRouter} from "next/navigation";
export default function Manage({params}:{params:{id:string}}){const r=useRouter();const [d,setD]=useState<any>(null);const [err,setErr]=useState("");useEffect(()=>{fetch("/api/demands").then(x=>x.json()).then(a=>{const v=a.find((x:any)=>String(x.id)===params.id);if(!v)r.push("/admin");else setD(v)})},[params.id]);if(!d)return <main className="container"><div className="card">Loading...</div></main>;
async function save(){const x=await fetch("/api/demands/"+d.id,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:d.status,response:d.response})});if(!x.ok){setErr((await x.json()).error);return}r.push("/admin")}
return <main className="container"><div className="form"><h1>Manage DM-{d.id}</h1><div className="card"><p><b>Customer:</b> {d.customer_name} ({d.customer_email})</p><p><b>Product:</b> {d.product}</p><p><b>Quantity:</b> {d.quantity}</p><p><b>Budget:</b> {d.budget||"—"}</p><p><b>Location:</b> {d.location}</p><p><b>Details:</b> {d.details}</p><div className="field"><label>Status</label><select value={d.status} onChange={e=>setD({...d,status:e.target.value})}><option>Pending</option><option>In Progress</option><option>Completed</option><option>Rejected</option></select></div><div className="field"><label>Admin Response</label><textarea value={d.response||""} onChange={e=>setD({...d,response:e.target.value})}/></div>{err&&<div className="notice danger">{err}</div>}<button className="btn" onClick={save}>Save Update</button></div></div></main>}