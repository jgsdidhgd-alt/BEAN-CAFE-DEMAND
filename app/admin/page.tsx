 "use client"; import Link from "next/link"; import {useEffect,useState} from "react";
export default function Admin(){const [ds,setDs]=useState<any[]>([]);useEffect(()=>{fetch("/api/demands").then(async x=>{if(x.status===401){location.href="/login";return}setDs(await x.json())})},[]);async function logout(){await fetch("/api/auth/logout",{method:"POST"});location.href="/"}return <main className="container"><div className="toprow"><div><h1>Admin Dashboard</h1><p className="muted">Real database se customer demands manage karein.</p></div><button className="btn alt" onClick={logout}>Logout</button></div><div className="grid"><div className="card"><span className="muted">Customers</span><div className="stat">{new Set(ds.map(d=>d.customer_name)).size}</div></div><div className="card"><span className="muted">Demands</span><div className="stat">{ds.length}</div></div><div className="card"><span className="muted">Pending</span><div className="stat">{ds.filter(d=>d.status==="Pending").length}</div></div></div><div className="card" style={{marginTop:22}}><h2>All Demands</h2><div className="tablewrap"><table className="table"><thead><tr><th>ID</th><th>Customer</th><th>Product</th><th>Qty</th><th>Location</th><th>Status</th><th>Action</th></tr></thead><tbody>{ds.map(d=><tr key={d.id}><td>DM-{d.id}</td><td>{d.customer_name}</td><td>{d.product}</td><td>{d.quantity}</td><td>{d.location}</td><td><span className="badge">{d.status}</span></td><td><Link className="btn" style={{padding:"7px 10px"}} href={"/admin/demand/"+d.id}>Manage</Link></td></tr>)}</tbody></table></div></div></main>}