 "use client"; import Link from "next/link"; import {useEffect,useState} from "react";
export default function Customer(){const [ds,setDs]=useState<any[]>([]);const [err,setErr]=useState("");
async function load(){const x=await fetch("/api/demands");const j=await x.json();if(!x.ok){location.href="/login";return}setDs(j)}
useEffect(()=>{load()},[]);
async function logout(){await fetch("/api/auth/logout",{method:"POST"});location.href="/"}
return <main className="container"><div className="toprow"><div><h1>Customer Dashboard</h1><p className="muted">Aapki demands aur admin responses.</p></div><div className="actions"><Link className="btn" href="/customer/new-demand">+ New Demand</Link><button className="btn alt" onClick={logout}>Logout</button></div></div><div className="grid"><div className="card"><span className="muted">My Demands</span><div className="stat">{ds.length}</div></div><div className="card"><span className="muted">Pending</span><div className="stat">{ds.filter(d=>d.status==="Pending").length}</div></div><div className="card"><span className="muted">Completed</span><div className="stat">{ds.filter(d=>d.status==="Completed").length}</div></div></div><div className="card" style={{marginTop:22}}><h2>My Demands</h2><div className="tablewrap"><table className="table"><thead><tr><th>ID</th><th>Product</th><th>Qty</th><th>Location</th><th>Status</th><th>Admin Response</th></tr></thead><tbody>{ds.map(d=><tr key={d.id}><td>DM-{d.id}</td><td>{d.product}</td><td>{d.quantity}</td><td>{d.location}</td><td><span className="badge">{d.status}</span></td><td>{d.response||"—"}</td></tr>)}</tbody></table></div></div></main>}