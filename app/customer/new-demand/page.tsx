 "use client"; import {useState} from "react"; import {useRouter} from "next/navigation";
export default function NewDemand(){const r=useRouter();const [f,setF]=useState({product:"",quantity:"",budget:"",location:"",requiredDate:"",details:""});const [err,setErr]=useState("");
async function submit(e:any){e.preventDefault();const x=await fetch("/api/demands",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});const j=await x.json();if(!x.ok){setErr(j.error);return}r.push("/customer")}
return <main className="container"><form className="card form" onSubmit={submit}><h1>New Demand</h1>{([["product","Product / Service"],["quantity","Quantity"],["budget","Budget"],["location","Location"],["requiredDate","Required Date"],["details","Details"]] as const).map(([k,l])=><div className="field" key={k}><label>{l}</label>{k==="details"?<textarea required value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>:<input required={["product","quantity","location"].includes(k)} type={k==="quantity"?"number":k==="requiredDate"?"date":"text"} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>}</div>)}{err&&<div className="notice danger">{err}</div>}<button className="btn">Submit Demand</button></form></main>}