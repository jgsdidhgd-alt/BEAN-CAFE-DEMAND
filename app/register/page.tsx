 "use client"; import {useState} from "react"; import {useRouter} from "next/navigation";
export default function Register(){const r=useRouter();const [f,setF]=useState({name:"",email:"",password:""});const [err,setErr]=useState("");
async function submit(e:any){e.preventDefault();const x=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});const j=await x.json();if(!x.ok){setErr(j.error);return}r.push("/customer")}
return <main className="container"><form className="card form" onSubmit={submit}><h1>Create Customer Account</h1>{(["name","email","password"] as const).map(k=><div className="field" key={k}><label>{k==="name"?"Full Name":k[0].toUpperCase()+k.slice(1)}</label><input required type={k==="password"?"password":k==="email"?"email":"text"} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/></div>)}{err&&<div className="notice danger">{err}</div>}<button className="btn">Create Account</button></form></main>}